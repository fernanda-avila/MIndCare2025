/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['source.unsplash.com', 'i.pravatar.cc', 'randomuser.me', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
